### Quetion
First, create a new StorageClass named local-path2 for an existing provisioner named rancher.io/local- Set the volume binding mode to WaitForFirstConsumer .Not setting the volume binding mode or setting it to anything other than WaitForFirstConsumer may result in a reduced score.
Next, configure the StorageClass local-path2 as the default StorageClass.


