#### etcd confs in the kube-apiserver.yaml is wrong. correct it. 
```bash
/etc/kubenetes/manifest
```
#### kubelet service is not running.. start it and enable it. 
### Use below for further troubleshooting... 

```bash
crictl ps -a
```
```bash
docker logs
```
